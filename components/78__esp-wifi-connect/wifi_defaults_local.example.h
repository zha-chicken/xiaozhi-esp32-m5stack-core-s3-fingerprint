#pragma once

// Copy this file to wifi_defaults_local.h and fill in local credentials
// before building if you want the firmware to auto-connect on first boot.
// wifi_defaults_local.h is gitignored because it may contain secrets.
#define XIAOZHI_DEFAULT_WIFI_SSID "YOUR_WIFI_SSID"
#define XIAOZHI_DEFAULT_WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
